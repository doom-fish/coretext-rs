[package]
name = "coretext-rs"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"
homepage = "https://github.com/doom-fish/coretext-rs"
repository = "https://github.com/doom-fish/coretext-rs"
description = "Safe Rust bindings for Apple's CoreText framework — fonts, lines, runs, frames, and attributed strings on macOS"
authors = ["Per Johansson <per@doom.fish>"]
keywords = ["coretext", "macos", "apple", "text", "font"]
categories = ["api-bindings", "graphics", "os::macos-apis"]
rust-version = "1.76"
readme = "README.md"
build = "build.rs"
include = [
    "src/**/*",
    "examples/**/*",
    "tests/**/*",
    "build.rs",
    "Cargo.toml",
    "README.md",
    "LICENSE-*",
    "CHANGELOG.md",
]

[lints.rust]

[lints.clippy]
pedantic = { level = "warn", priority = -1 }
nursery = { level = "warn", priority = -1 }
cargo = { level = "warn", priority = -1 }

[lib]
name = "coretext"
crate-type = ["lib"]

[dependencies]
libc = "0.2"

[dev-dependencies]

[package.metadata.docs.rs]
all-features = true
default-target = "aarch64-apple-darwin"
targets = ["aarch64-apple-darwin", "x86_64-apple-darwin"]
rustdoc-args = ["--cfg", "docsrs"]

[badges]
maintenance = { status = "actively-developed" }

[[example]]
name = "01_layout_smoke"

[profile.release]
debug = "full"
strip = "none"
